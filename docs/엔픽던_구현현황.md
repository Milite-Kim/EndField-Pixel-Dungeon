# 엔픽던 (EndField Pixel Dungeon) 구현 현황

> 베이스: Shattered Pixel Dungeon (Evan Debenham)
> 레포: `Milite-Kim/EndField-Pixel-Dungeon`
> 개발 브랜치: `claude/review-enpicture-docs-slOd8`

---

## Phase 1 — 핵심 전투 시스템 ✅ 완료

### 1-1. 데미지 타입 확장

**파일**: `actors/DamageType.java`

```
PHYSICAL, HEAT, COLD, NATURE, ELECTRIC
```

- 기존 Shattered PD의 단일 물리 피해를 5종 속성으로 확장
- `Char.damage(int, Object, DamageType)` 오버로드 추가

---

### 1-2. 아츠 부착 스택 시스템

**파일**: `actors/buffs/ArtsAttachment.java`

| 상황 | 동작 |
|------|------|
| 첫 부착 | 해당 속성 1스택 생성 |
| 동일 속성 재부착 | **아츠 폭발** — `attacker.damageRoll() × stacks × 0.5` 속성 피해 + 스택 +1 |
| 다른 속성 부착 | **아츠 반응** — 기존 스택 전량 소모 후 상태이상 적용 |

**아츠 반응 결과**

| 반응 | 결과 버프 |
|------|----------|
| 열기 | `Combustion` — 소모 스택 비례 열기 지속 피해 |
| 냉기 | `Frozen` — 소모 스택 비례 이동 불가 (1스택=1턴, 2~3스택=2턴, 4스택=3턴) |
| 자연 | `ArtsCorrosion` — 해당 적 공격 시 공격자 소량 회복 |
| 전기 | `Electrified` — 지속 중 받는 아츠 피해 증가 |

**TODO**: 폭발/반응 수치 확정 (현재 임시값)

이벤트 훅: `apply()` 완료 후 `Dungeon.hero.checkChainTriggers(enemy)` 호출 ✅

---

### 1-3. 방어불능 스택 시스템

**파일**: `actors/buffs/DefenselessStack.java`

물리 이상 4종을 `DefenselessStack.apply(enemy, type, attacker)` 단일 진입점으로 관리.

| 종류 | 첫 적용 | 스택 있을 때 |
|------|--------|------------|
| `LAUNCH` (띄우기) | +1스택 | +1스택 + `damageRoll × 1.0` 물리 피해 |
| `KNOCKDOWN` (넘어뜨리기) | +1스택 | +1스택 + `damageRoll × 0.7` 물리 피해. 4스택 도달 시 넉백 1칸(TODO) |
| `HEAVY_ATTACK` (강타) | +1스택 | 전량 소모 + `damageRoll × stacks × 1.5` 물리 피해 |
| `ARMOR_BREAK` (갑옷파괴) | +1스택 | 전량 소모 + `damageRoll × stacks × 0.5` 물리 피해 + `ArmorBreaked` 디버프 |

**쇄빙**: `apply()` 진입 시 대상이 `Frozen` 상태이면 동결 소모 + `damageRoll × 3.0` 대량 물리 피해 → 이후 일반 스택 처리 계속

**TODO**: 모든 배율 수치 확정 / 넉백 이동 시스템 연동

이벤트 훅: `apply()` 완료 후 `Dungeon.hero.checkChainTriggers(enemy)` 호출 ✅

---

### 1-4. 상태이상 버프 목록

| 버프 | 파일 | 주요 기능 |
|------|------|----------|
| `ArtsCorrosion` | `buffs/ArtsCorrosion.java` | `healOnHit()` — 공격 시 공격자 회복 (확률/비율 조정 가능) |
| `Frozen` | `buffs/Frozen.java` | `isFrozen()` — 이동 불가 / `apply(enemy, stacks)` — 스택 비례 지속 |
| `Combustion` | `buffs/Combustion.java` | 스택 비례 열기 지속 피해 |
| `Electrified` | `buffs/Electrified.java` | 받는 아츠 피해 증가 |
| `ArmorBreaked` | `buffs/ArmorBreaked.java` | 스택 비례 받는 물리 피해 증가 |
| `Vulnerable` | `buffs/Vulnerable.java` | 범용 취약 (속성별 변형 가능) |

---

### 1-5. Char.java 훅 추가

**파일**: `actors/Char.java`

`enemy.damage(effectiveDamage, this)` 직후:

```java
// 부식 heal-on-hit
ArtsCorrosion corrosion = enemy.buff(ArtsCorrosion.class);
if (enemy.isAlive() && corrosion != null) {
    int heal = corrosion.healOnHit();
    HP = Math.min(HP + heal, HT);
}
```

**TODO**: 회복 플로팅 텍스트 표시

---

## Phase 2 — 오퍼레이터 & 연계기 시스템 ✅ 완료

### 2-1. 오퍼레이터 기반 구조

| 파일 | 설명 |
|------|------|
| `operators/Operator.java` | 직군 6종, 무기 5종, 속성 5종, 콤보 배율, 저장/불러오기 |
| `operators/TeamOperator.java` | 쿨타임 관리, `chainCondition()` / `activateChain()` 추상 메서드 |
| `operators/BattleSkill.java` | 쿨타임 방식 / `range()` / `castTime()` / `autoApproach()` / `canTargetCell()` / `selfTarget()` |
| `operators/Ultimate.java` | 충전 방식 / `range()` / `castTime()` / `autoApproach()` / `canTargetCell()` / `selfTarget()` / 이벤트별 충전량 메서드 |
| `operators/OperatorRegistry.java` | 해금 시스템, 헤드헌팅 풀, 파티 관리, 영구 저장 (operators.dat) |

---

### 2-2. 연계기 큐 시스템

**파일**: `operators/ChainQueue.java`

#### 타이머 방식
`Actor.now()` 절대 게임 시간 기반. 이속/공속 버프와 무관하게 활성화 시점으로부터 정확히 `DEFAULT_WINDOW(3f)` 게임 시간 후 만료.

#### 큐 순서 규칙

1. **도착 순서** 기반 FIFO — 조건이 먼저 충족된 연계기가 앞
2. **동일 타이밍** 충족 시 파티 순서 우선
3. **이미 큐에 있는 오퍼레이터 재충족** → 현재 위치 유지, 만료 시간만 갱신
4. **만료된 항목** 자동 폐기

---

### 2-3. 배틀스킬 발동 입력 시스템

**파일**: `HeroAction.UseBattleSkill`, `Hero.java`

| 메서드 | 설명 |
|--------|------|
| `enterBattleSkillTargeting()` | 버튼 첫 클릭. `selfTarget` 즉시 발동 / 더블클릭 즉시 발동 / 타겟팅 모드 진입 |
| `confirmBattleSkillTarget(cell)` | 타겟 셀 확정 → `UseBattleSkill` 액션 세팅 |
| `cancelBattleSkillTargeting()` | ESC 등 취소 |
| `actBattleSkill()` | 사거리 체크 → 발동 / 접근 / 취소 처리 + `castTime()` 소모 |

---

### 2-4. 궁극기 충전 훅

**파일**: `Ultimate.java`, `Hero.java`

| 이벤트 | 메서드 | 훅 위치 |
|--------|--------|---------|
| 강력한 일격 적중 | `chargePerFinishingBlow()` | `onFinishingBlowLanded()` |
| 배틀스킬 사용 | `chargePerBattleSkill()` | `actBattleSkill()` |
| 연계기 발동 | `chargePerChain()` | `activateFrontChain()` |

오퍼레이터 서브클래스에서 오버라이드. 기본값 0 = 해당 이벤트로 충전 안 됨.
상태 조건 기반 충전(포그라니치니크 등)은 각 스킬 `activate()` 내에서 `addChargeInternal()` 처리.

---

### 2-5. 궁극기 발동 입력 시스템

**파일**: `HeroAction.UseUltimate`, `Hero.java`

배틀스킬과 동일한 타겟팅 구조.

| 메서드 | 설명 |
|--------|------|
| `enterUltimateTargeting()` | `selfTarget` 즉시 발동 / 더블클릭 / 타겟팅 모드 진입 |
| `confirmUltimateTarget(cell)` | 타겟 셀 확정 → `UseUltimate` 액션 세팅 |
| `actUltimate()` | 사거리 체크 → 발동 / 접근 / 취소 + `castTime()` 소모 |

---

### 2-6. 파티 시스템 (Hero.java)

| 항목 | 상태 |
|------|------|
| `activeMainOperator` + `setMainOperator()` | ✅ |
| `teamOperators` + `addTeamOperator()` / `replaceTeamOperator()` | ✅ |
| `activeBattleSkill` / `activeUltimate` 동기화 | ✅ |
| `chainQueue` + 매 턴 `tick()` | ✅ |
| `checkChainTriggers()` / `activateFrontChain()` | ✅ |
| 저장/불러오기 | ✅ |

---

### 2-7. 구현된 오퍼레이터

#### 진천우 (Jincheonwoo) — `operators/team/Jincheonwoo.java`

| 항목 | 상태 |
|------|------|
| 배틀스킬 구조 | ✅ |
| 배틀스킬 실제 피해 (×1.5) | ✅ (수치 TODO) |
| 연계기 조건 | ✅ |
| 연계기 피해 (×1.2) + LAUNCH | ✅ (수치 TODO) |
| 연계기 관통 이동 (메인 시만) | ✅ |
| 궁극기 7회 피해 (×0.8/회) | ✅ (수치 TODO) |
| 궁극기 히트 간 애니메이션 시퀀스 | 🔴 TODO |
| 궁극기 승급 확장 | 🔴 TODO |

---

### 2-8. 헤드헌팅 아이템

**파일**: `items/HeadhuntingPermit.java`

- `AC_USE` 액션 구현, Fisher-Yates 셔플 4명 후보 제시 ✅
- **TODO**: UI 연결, 스프라이트, 리롤 비용

---

## Phase 3 — 콘텐츠 & 최소 UI 🟡 진행 중

> UI는 마무리 단계 방침이나, 배틀스킬/궁극기/연계기 버튼은 게임 흐름 테스트에 필수이므로 초반에 최소 구현

### 우선순위

| 순위 | 항목 | 상태 |
|------|------|------|
| ① | **전투 UI 최소 구현** | ✅ 완료 |
| ② | **오퍼레이터 선택 화면** | ✅ 완료 |
| ③ | **이동 시스템 연동** | ✅ 완료 |
| ④ | **오퍼레이터 구현** | ✅ 진천우 완료 |
| ⑤ | **기존 SPD 시스템 정리** | 🔴 미착수 |
| ⑥ | **헤드헌팅 UI** | 🔴 미착수 |
| ⑦ | **몬스터/적 설계** | 🔴 미착수 |
| ⑧ | **밸런스 수치 확정** | 🔴 미착수 |

---

### Phase 3 상세

#### ① 전투 UI 최소 구현
- 배틀스킬 버튼 (쿨타임 표시 포함)
- 궁극기 버튼 + 충전 게이지
- 연계기 버튼 (큐 헤드 오퍼레이터 + 남은 시간 표시)
- 타겟팅 모드 시 취소 버튼

#### ② 오퍼레이터 선택 화면 ✅
- 던전 시작 시 메인 오퍼레이터 선택
- 팀 오퍼레이터 선택 (해금된 것 중)
- 첫 실행 기본 구성 (관리자 메인 + 펠리카 팀 유력)

#### ③ 이동 시스템 연동 ✅
- 넉백 1칸 (넘어뜨리기 4스택) — `DefenselessStack.knockback()` ✅
- 관통 이동 (진천우 연계기 — 적 통과 후 뒤에 위치) ✅
  - **조건**: 진천우가 메인 오퍼레이터일 때만 이동 수행

#### ④ 오퍼레이터 구현 ✅ (진천우)
- 진천우 배틀스킬 피해(×1.5) + LAUNCH ✅ (수치 TODO)
- 진천우 연계기 피해(×1.2) + LAUNCH + 관통이동(메인 시) ✅ (수치 TODO)
- 진천우 궁극기 7회 피해(×0.8/회) ✅ (수치 TODO, 히트 시퀀스 애니 TODO)
- 이후 속성별 묶음 구현 권장 순서:
  - 물리 → 냉기 → 전기 → 열기 → 자연

#### ⑤ 기존 SPD 시스템 정리
- `Burning` → `Combustion` 교체 및 기존 호출부 제거
- `Corrosion` → `ArtsCorrosion` 교체
- `Frost` → `Frozen` 교체

#### ⑥ 헤드헌팅 UI
- 4명 후보 표시 창
- 선택 / 리롤(크레디트 소모) / 포기
- 만원 시 교체 선택

#### ⑦ 몬스터/적 설계
- 속성 부착 반응 확인용 기본 적
- 차지(charge) 패턴 보유 적 (카치크/스노우샤인/엠버 연계기 트리거)

#### ⑧ 밸런스 수치 확정

모든 `TODO` 임시값 → 확정값 교체

---

## 미확정 수치 목록

| 항목 | 임시값 | 위치 |
|------|--------|------|
| 아츠 폭발 배율 | `0.5f` | `ArtsAttachment.EXPLOSION_MULT` |
| 쇄빙 배율 | `3.0f` | `DefenselessStack.SHATTERING_DMG_MULT` |
| 띄우기 피해 배율 | `1.0f` | `DefenselessStack.LAUNCH_DMG_MULT` |
| 넘어뜨리기 피해 배율 | `0.7f` | `DefenselessStack.KNOCKDOWN_DMG_MULT` |
| 강타 피해 배율 | `1.5f` | `DefenselessStack.HEAVY_ATTACK_DMG_MULT` |
| 갑옷파괴 피해 배율 | `0.5f` | `DefenselessStack.ARMOR_BREAK_DMG_MULT` |
| 연계기 유효 시간 | `3f` | `ChainQueue.DEFAULT_WINDOW` |
| 부식 회복량/확률 | TODO | `ArtsCorrosion.healOnHit()` |
| 연계기 쿨타임 수치 | TODO | 각 오퍼레이터 `baseCooldown()` |
| 궁극기 최대 충전량 | TODO | 각 오퍼레이터 `maxCharge()` |
| 오퍼레이터별 궁극기 이벤트 충전량 | TODO | 각 오퍼레이터 `chargePerFinishingBlow()` 등 |
